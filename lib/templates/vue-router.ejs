// 懒加载路由
const Home = () => import('./<%= data.lowerName %>.vue')
export default {
  path: '/<%= data.lowerName %>', 
  name: '<%= data.lowerName %>',
  component: <%= data.name %>,
  children: [
  ]
}
